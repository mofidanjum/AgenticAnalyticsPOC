# Streamlit Secrets Configuration
# Set these in Streamlit Cloud: Settings → Secrets

# Anthropic API Key (required for Claude AI features)
ANTHROPIC_API_KEY = ""

# Kaggle Credentials (optional, only needed for data refresh)
KAGGLE_USERNAME = ""
KAGGLE_API_TOKEN = ""
